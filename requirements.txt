# torch/torchvision 请用官方 CUDA index 单独装（见 README / 实现方案 Step 0）：
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
open_clip_torch
faiss-cpu
numpy
scikit-learn
pandas
matplotlib
tqdm
pyyaml
huggingface_hub          # 取数据（scripts/00_fetch_data.py）；数据落盘后运行期不再需要
pillow
# Phase 2（可选）：
# peft
# accelerate
